# Node modules should not be copied (Docker installs its own)
node_modules

# Ignore TypeScript build outputs (Docker builds inside container)
dist
build

# Environment files should not be included in image
.env
.env.*
!env.example

# Prevent logs from being copied
npm-debug.log
yarn-debug.log
yarn-error.log

# Git repository info not needed in Docker image
.git
.gitignore

# Docker files not needed inside image
Dockerfile*
docker-compose*

# OS and editor-specific garbage
.DS_Store
*.swp
*.lock

# Temporary files
tmp
cache
